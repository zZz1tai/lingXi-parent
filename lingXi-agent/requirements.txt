# Web Framework
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0
sse-starlette>=2.0.0,<3.0.0

# Pydantic & Settings
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0

# LangChain v1 ecosystem. Pin the verified set because the runtime-aware
# summarization adapter necessarily integrates with middleware internals; bump
# these together only after the offline v1 contract suite passes.
langchain==1.3.14
langchain-core==1.4.9
langchain-openai==1.3.5
langgraph==1.2.9

# Durable short-term memory.  Development/tests use InMemorySaver; production
# can select AsyncPostgresSaver through the lifecycle helper in app/agents.
langgraph-checkpoint-postgres==3.1.0
psycopg[binary,pool]>=3.2.0,<4.0.0

# Search
tavily-python>=0.5.0,<1.0.0

# HTTP Client
httpx>=0.27.0,<1.0.0

# Environment
python-dotenv>=1.0.0,<2.0.0
